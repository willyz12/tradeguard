/**
 * Session 1 stub entrypoint.
 *
 * This will become a small local MCP server in Session 2, exposing one tool
 * (e.g. `check_order`) that skill/SYSTEM_PROMPT.md is required to call before
 * ever presenting an order for confirmation. For now it just confirms the
 * package runs.
 */
console.log(
  "[guard] Session 1 skeleton only - no enforcement logic yet. See SESSION_REPORT.md."
);
